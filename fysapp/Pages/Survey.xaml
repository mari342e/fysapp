<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:views="fysapp.NavigationBar" x:Class="fysapp.Pages.Survey" xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core" ios:Page.UseSafeArea="true">
    <ContentPage.Content>
        <StackLayout BackgroundColor="{StaticResource bgColor}">
            <StackLayout Margin="20,20,20,0" VerticalOptions="StartAndExpand">
                <Frame Style="{StaticResource backBtn}">
                    <Frame.GestureRecognizers>
                        <TapGestureRecognizer Tapped="GoBack" NumberOfTapsRequired="1" />
                    </Frame.GestureRecognizers>
                    <Image Source="Back" Style="{StaticResource backIcon}" />
                </Frame>
                <Label Text="Spørgeskema" Style="{StaticResource h1Back}" />
                <Label Text="Der er i øjeblikket ingen spørgeskemaer at udfylde." TextColor="{StaticResource bodyText}" />
            </StackLayout>
            <views:NavigationBar />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
